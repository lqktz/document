对的单个模块的编译过程：
source ./build/envsetup.sh
choosecombo 1 pixi474gtmo 3 2 2 2 2 2 2 2

3 2 2 2 2 2 2 2代表对choosecombo的选项

如果是对整个项目编译则是使用make -j8或者make -j16

若是对单个模块进行单个编译：
使用mmm packages/apps/Settings/
packages/apps/Settings/是Android.mk文件所在的路径，编译是使用Android.mk编译的。
编译需要一定的时间，编译的结果在out/target/product/pixi474gtmo/system/priv-app/Settings/

先要进行：
adb kill-server
adb devices
需要关闭虚拟机，否则adb devices会失败。
小贴士：当使用adb shell后就是进入手机了，使用ls后显示的是手机的目录结构，等下APK文件要push到的位置就是在这里可以看到/system/priv-app/Settings

cd到该目录下，
adb remount
adb push Settings.apk /system/priv-app/Settings

push成功后需要是重启机器就行。
